class FeatureFlags {
  FeatureFlags._();

  /// Activează funcționalitatea de donații (tab + card pe Home).
  /// Schimbă în `true` când ești gata să o lansezi.
  static const bool donationsEnabled = false;

  /// Activează funcționalitatea de cerere acatist (card pe Home).
  /// Schimbă în `true` când ești gata să o lansezi.
  static const bool acatistRequestEnabled = true;
}
